@model IEnumerable<WebProject.Data.Entity.Vid_sporta>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-dark">Список видов спорта</h2>
        </div>
        @if (User.IsInRole("admin") || User.IsInRole("moderator"))
        {
            <div class="col-6 text-right">
                <a asp-controller="Vid_sporta" asp-action="Create" class="btn btn-primary">Создать "вид спорта"</a>
            </div>
        }
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Название
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var vidsporta in Model)
                {
                <tr>
                    <td width="25%">@vidsporta.Name</td>
                    @if (User.IsInRole("admin") || User.IsInRole("moderator"))
                    {
                        <td width="25%">
                            <div class="w-100 btn-group" role="group">
                                <a asp-controller="Vid_sporta" asp-action="Update" asp-route-Id="@vidsporta.Id" class="btn btn-success mx-1">Изменить</a>
                                <a asp-controller="Vid_sporta" asp-action="Delete" asp-route-Id="@vidsporta.Id" class="btn btn-danger mx-1">Удалить</a>
                            </div>
                        </td>
                    }
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No items created yet</p>
    }

</div>

