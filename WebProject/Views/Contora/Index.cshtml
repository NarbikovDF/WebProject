@model IEnumerable<WebProject.Data.Entity.Contora>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-dark">Список контор</h2>
        </div>
        @if (User.IsInRole("admin") || User.IsInRole("moderator"))
        {
            <div class="col-6 text-right">
                <a asp-controller="Contora" asp-action="Create" class="btn btn-primary">Создать "контору"</a>
            </div>
        }
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Город
                    </th>
                    <th>
                        Улица
                    </th>
                    <th>
                        Номер дома
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var contora in Model)
                {
                <tr>
                    <td width="25%">@contora.City</td>
                    <td width="25%">@contora.Street</td>
                    <td width="25%">@contora.House</td>
                    @if (User.IsInRole("admin") || User.IsInRole("moderator"))
                    {
                        <td width="25%">
                            <div class="w-100 btn-group" role="group">
                                <a asp-controller="Contora" asp-action="Update" asp-route-Id="@contora.Id" class="btn btn-success mx-1">Изменить</a>
                                <a asp-controller="Contora" asp-action="Delete" asp-route-Id="@contora.Id" class="btn btn-danger mx-1">Удалить</a>
                            </div>
                        </td>
                    }
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No items created yet</p>
    }

</div>

